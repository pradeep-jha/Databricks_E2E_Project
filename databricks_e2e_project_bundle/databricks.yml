# This is a Databricks asset bundle definition for databricks_e2e_project_bundle.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: databricks_e2e_project_bundle
  uuid: db67558c-c247-4c67-8af6-cb2368673cf5

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below----.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use
  warehouse_id:
    description: The warehouse-id to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy....
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-672bb3e2-e03d.cloud.databricks.com
    variables:
      catalog: dev
      schema: dev
      warehouse_id: 3be3918113e8fce9
    presets:
      artifacts_dynamic_version: true
  prod:
    mode: production
    workspace:
      host: https://dbc-672bb3e2-e03d.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/pradeep.jha1987@gmail.com to make sure we only have a single copy..
      #root_path: /Workspace/Users/pradeep.jha1987@gmail.com/.bundle/${bundle.name}/${bundle.target}
      root_path: /Workspace/PROD_Databricks_E2E_Project
    variables:
      catalog: prod
      schema: prod
      warehouse_id: 3be3918113e8fce9
    permissions:
      - user_name: pradeep.jha1987@gmail.com
        level: CAN_MANAGE
